/** @deprecated Import from `./roomScopes` — kept so older imports keep working */
export {
  getRoomScopeSections,
  ROOM_SCOPE_REGISTRY,
  type ScopeSection,
  type ScopeField,
  type ScopeFieldType,
} from './roomScopes';
